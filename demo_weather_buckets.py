#!/usr/bin/env python3
"""
Legacy demo showcasing weather bucket logic.

Weather buckets are now handled entirely on the frontend (see
app/api/weather/route.ts). This script is preserved only to point to the new
implementation.
"""

raise RuntimeError(
    "demo_weather_buckets.py is deprecated. Weather bucket logic lives in the "
    "Next.js API routes now."
)
